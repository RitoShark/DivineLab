# HUD Editor (Archived)

This feature has been removed from the main application and archived here for reference.

## Files Archived

- `pages/HUDEditor.js` - Main HUD Editor page component
- `pages/HUDEditor.css` - HUD Editor styles
- `components/HUDSimulator.js` - HUD Simulator component
- `components/HUDSimulator.css` - HUD Simulator styles  
- `utils/hudParser.js` - HUD parsing utilities

## Removal Date

Removed: [Current Date]

## Reason for Removal

Removed from main application as it was no longer needed/used.

## Note

To restore this feature, move these files back to their original locations in the `src/` directory and re-enable references in:
- `src/App.js` (import and route)
- `src/components/ModernNavigation.js` (navigation item)
- `src/pages/Settings.js` (visibility toggle)

